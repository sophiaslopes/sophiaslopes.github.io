{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome with Dark Mode",
            "userDataDir": "${workspaceFolder}/.vscode/chrome",
            "args": [
                "--force-dark-mode"
            ]
        }
    ]
}
